snippet     pass
abbr        pass password entry
options     head
    ${1:`substitute(system('apg -m 30 -n 1 -M SCNL'), '\n', '', '')`}
    ---
    user: ${2:username}
    window: ${3:`substitute(expand('%:t:r'), '.*-', '', '')`}${0}

snippet     ansible_secret
abbr        ansible secret
    `system('apg -m 30 -n 1 -M SCNL | tr -d "\n" | ansible-vault encrypt_string --vault-id vault-password.py 2>/dev/null')`
